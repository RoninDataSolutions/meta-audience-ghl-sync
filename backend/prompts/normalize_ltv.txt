I have {count} LTV values. Calculate the percentile rank (0-100) for each where 100=highest, 0=lowest.

Values: {values}

Rules:
- Return ONLY a JSON array of integers, nothing else
- The array MUST have exactly {count} elements — one per input value, in the same order
- No explanation, no markdown, no extra text